// Step 3 of the data pull (see SPEC.md "Core logic").
// Normalizes raw ODPT + Overpass output into the Station/Exit/POI schema
// (see SPEC.md "Data model") and writes to ../data as local JSON.

async function normalize() {
  throw new Error('not implemented yet')
}

normalize()
